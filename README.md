# Bulk_Mailer
This tool automatically sends emails from an Excel list, allowing you to replace recipient names in the email content. The email content is sent via an HTML form, resulting in faster, more automated, visually appealing, and professional emails.

